import React, { useState, useEffect, useContext } from "react";
import styles from "../css/login.module.css";

const Login = (props) => {
  return <div className={styles.layout}>login</div>;
};

export default Login;
